# MyDictionary - 测试指南

## 当前状态

### ✅ 已完成
- 基础项目结构
- Manifest V3 配置
- 依赖安装成功 (@xenova/transformers)
- Background Service Worker 代码
- Content Script 代码
- UI 样式
- **Vite 构建配置完成**
- **图标文件创建完成** (16x16, 48x48, 128x128)
- **生产版本已构建** (dist/ 目录, 40MB)

### ⚠️ 待测试
1. **Chrome 扩展加载** - 在 chrome://extensions/ 加载 dist/ 目录
2. **模型下载** - 测试首次翻译时自动下载
3. **翻译功能** - 测试实际翻译效果

## 如何加载测试

### 生产版本测试 (推荐)

1. **打开 Chrome 扩展管理页面**
   ```
   chrome://extensions/
   ```

2. **开启"开发者模式"** (右上角开关)

3. **点击"加载已解压的扩展程序"**

4. **选择 dist 目录**
   ```
   /Users/jason/Dev/crypto-projects/MyDictionary/dist
   ```

5. **验证加载成功**
   - 扩展列表中显示 "MyDictionary" 🦝
   - 没有图标错误警告
   - 点击 "Service Worker" 查看后台日志

### 开发模式测试 (可选)

#### 快速创建图标 (使用在线工具):

1. 访问 https://www.favicon-generator.org/
2. 上传一个浣熊图片或直接生成
3. 下载生成的图标
4. 重命名为:
   - `icon-16.png`
   - `icon-48.png`
   - `icon-128.png`
5. 放到 `assets/icons/` 目录
6. 重新加载插件

#### 或使用 Emoji 图标:

你可以手动创建一个简单的 HTML 文件生成图标:

```html
<!DOCTYPE html>
<html>
<body>
  <canvas id="c" width="128" height="128"></canvas>
  <script>
    const c = document.getElementById('c');
    const ctx = c.getContext('2d');
    ctx.fillStyle = '#667eea';
    ctx.fillRect(0, 0, 128, 128);
    ctx.font = '90px Arial';
    ctx.textAlign = 'center';
    ctx.textBaseline = 'middle';
    ctx.fillText('🦝', 64, 64);

    // 右键点击画布 → 另存为图片
  </script>
</body>
</html>
```

## 测试步骤

### 1. 基础测试

**测试插件是否加载成功:**
```
1. 加载插件后,检查 chrome://extensions/ 是否显示 MyDictionary
2. 点击插件图标,查看 popup 是否打开
3. 查看 Service Worker 是否运行 (点击 "Service Worker" 链接查看 console)
```

**预期结果:**
- Popup 正常打开
- Service Worker console 显示: "🦝 MyDictionary Background Service Worker 已启动"

### 2. 测试侧边栏

**打开侧边栏:**
```
1. 在任意网页选中一段文本
2. 按 Ctrl+Shift+F (Mac: Cmd+Shift+F)
3. 查看右侧是否滑出侧边栏
```

**预期结果:**
- 侧边栏从右侧滑入
- 显示输入框和翻译按钮

### 3. 测试翻译功能

**首次翻译 (会触发模型下载):**
```
1. 在侧边栏输入英文文本,例如: "Hello world"
2. 点击"翻译"按钮
3. 观察输出区域
```

**预期行为:**
- 首次使用会显示: "⚠️ 模型未安装"
- 提示下载 Helsinki-NLP/opus-mt-en-zh (300MB)
- 点击"立即下载"开始下载
- 下载进度显示在 Service Worker console
- 下载完成后自动执行翻译
- 显示中文翻译结果

### 4. 检查 Console 输出

**Service Worker Console:**
```
chrome://extensions/ → 点击 MyDictionary 的 "Service Worker" 链接
```

**预期日志:**
```
🦝 MyDictionary - 初始化中...
✅ 配置文件加载成功
✅ ModelManager 初始化完成
🦝 MyDictionary Background Service Worker 已启动
📨 收到消息: translate
🔄 翻译请求: en → zh
⚠️ 模型 translation-en-zh 未安装
... (或者如果已安装)
📦 开始加载模型: Helsinki-NLP/opus-mt-en-zh
📥 下载进度: 10.5%
...
✅ 模型加载完成: translation-en-zh
✅ 翻译完成 (耗时: XXXms)
```

**Content Script Console (网页 DevTools):**
```
F12 → Console 标签
```

**预期日志:**
```
🦝 MyDictionary Content Script 已加载
✅ MyDictionary Content Script 初始化完成
📨 Content 收到消息: toggleSidebar
📝 选中文本: ...
```

## 常见问题

### Q1: 插件加载失败,提示 "Manifest file is missing or unreadable"
**解决方案:** 检查 manifest.json 是否有语法错误

### Q2: Service Worker 无法启动
**解决方案:**
1. 检查 background.js 第一行的 import 语句
2. 确保 node_modules/@xenova/transformers 已安装
3. 查看 Service Worker console 的错误信息

### Q3: 侧边栏无法打开
**解决方案:**
1. 检查 Content Script 是否注入成功 (在网页 DevTools → Console 查看)
2. 检查 src/ui/sidebar.css 是否存在
3. 尝试刷新网页

### Q4: 模型下载失败
**可能原因:**
1. 网络问题 (Hugging Face 在国外,可能需要代理)
2. 磁盘空间不足
3. 浏览器缓存限制

**解决方案:**
- 检查网络连接
- 查看 Service Worker console 的详细错误信息
- 清除浏览器缓存后重试

### Q5: 翻译结果不正确
**检查:**
1. 模型是否下载完整
2. 输入文本的语言是否正确检测
3. 查看 Service Worker console 的翻译日志

## 调试技巧

### 1. 重新加载插件
```
chrome://extensions/ → 点击 MyDictionary 的 "重新加载" 图标
```

### 2. 查看存储数据
```
chrome://extensions/ → MyDictionary → 详情 → 应用数据 → 存储数据
```

或在 Service Worker console 执行:
```javascript
chrome.storage.local.get(null, console.log)
```

### 3. 清除模型缓存
```javascript
// 在 Service Worker console 执行
chrome.storage.local.clear()
// 然后重新加载插件
```

### 4. 手动测试翻译
```javascript
// 在 Service Worker console 执行
chrome.runtime.sendMessage({
  action: 'translate',
  text: 'Hello world',
  sourceLang: 'en',
  targetLang: 'zh'
}, console.log)
```

## 性能监控

### 模型下载时间
- 300MB 模型: 约 30-120 秒 (取决于网络)
- 首次加载: 约 3-5 秒

### 翻译延迟
- 目标: < 1 秒
- 实际: 取决于文本长度和设备性能

### 内存占用
- 无模型: ~50MB
- 已加载翻译模型: ~350MB
- 完整加载: ~500MB

## 下一步

如果基础功能测试通过:
1. 添加近义词功能
2. 添加例句功能
3. 添加学术写作模式
4. 优化 UI/UX
5. 添加更多语言支持

---

**最后更新:** 2025-11-29
