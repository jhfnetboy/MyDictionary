# 🧪 测试本地文件导入功能

## 前提条件

- ✅ 已完成选项 A (爬取 2,500+ 短语)
- ✅ 已完成选项 B (实现导入 UI)
- ⚠️ **必须**: 移除并重新加载扩展

---

## 测试步骤

### 1. 准备测试 JSON 文件

创建一个测试文件 `test-phrases.json`:

```json
{
  "sections": {
    "test": {
      "custom": [
        {
          "id": "test_custom_1",
          "phrase": "This is a test phrase for import functionality",
          "usage": "测试导入功能",
          "academicScore": 7.5,
          "frequency": "high",
          "examples": ["This is a test phrase for import functionality"],
          "section": "test",
          "subsection": "custom"
        },
        {
          "id": "test_custom_2",
          "phrase": "Another test phrase to verify the import works correctly",
          "usage": "验证导入是否正常工作",
          "academicScore": 8.0,
          "frequency": "medium",
          "examples": ["Another test phrase to verify the import works correctly"],
          "section": "test",
          "subsection": "custom"
        }
      ]
    }
  },
  "citations": {},
  "transitions": {}
}
```

保存为 `test-phrases.json`

### 2. 移除并重新加载扩展

1. `chrome://extensions/`
2. 移除 MyDictionary
3. 重新加载扩展

### 3. 打开 Academic Writing 标签

1. 打开任意网页
2. 选中文本
3. `Cmd+Shift+D` (Mac) 或 `Ctrl+Shift+D` (Windows)
4. 点击 "Academic Writing" 标签

### 4. 验证 UI 显示

应该看到:

```
📚 Academic Phrasebank
Download 2,500+ academic phrases from University of Manchester

📦 Size: ~1.1 MB
📊 Phrases: 2,500+

[📥 Download Now]

─────── or ───────

📂 Import Local File
Import your own academic phrases from JSON file

[📁 Select JSON File]
```

### 5. 测试下载功能 (可选)

- 点击 "📥 Download Now"
- 等待 3-5 秒
- 应该显示: "✅ Successfully downloaded academic database!"
- 自动加载短语列表

### 6. 测试导入功能 (主要测试)

#### 测试 6.1: 成功导入

1. 点击 **"📁 Select JSON File"** 按钮
2. 选择之前创建的 `test-phrases.json`
3. **预期结果**:
   - 按钮变为 "⏳ Importing..."
   - 显示加载动画
   - 3 秒后显示: "✅ Successfully imported phrases! (2 phrases)"
   - 自动加载短语列表

4. 验证短语已导入:
   - 在 Section 选择器应该看到 "test" (如果下拉选项支持)
   - 搜索 "test phrase"
   - 应该找到 2 个测试短语

#### 测试 6.2: 无效 JSON

创建一个无效的 JSON 文件 `invalid.json`:

```
{ this is not valid json }
```

1. 点击 "📁 Select JSON File"
2. 选择 `invalid.json`
3. **预期结果**:
   - 显示错误: "❌ Import failed: Invalid JSON format"
   - 按钮恢复为 "📁 Select JSON File"

#### 测试 6.3: 多次导入 (累积)

1. 第一次导入 `test-phrases.json` (2 个短语)
2. 再次导入 `test-phrases.json`
3. **预期结果**:
   - 成功导入 (累积,不覆盖)
   - 总短语数应该增加

### 7. 检查 Service Worker Console

1. `chrome://extensions/`
2. 点击 MyDictionary 的 "Service Worker"
3. 查看日志

**预期日志**:

```
📥 开始导入本地学术短语...
✅ 本地文件导入完成,共 2 条短语
```

### 8. 检查 IndexedDB

1. 打开任意网页
2. F12 → Application → IndexedDB
3. 展开 "MyDictionary_Academic" → "phrases"
4. 应该看到:
   - 官方 2,500+ 短语 (如果已下载)
   - 测试短语 (test_custom_1, test_custom_2)

---

## 中文界面测试

1. 点击顶部 "English" 切换到中文
2. 验证导入界面翻译:

```
📚 学术短语库
下载 2500+ 个曼彻斯特大学学术写作短语

📦 大小: ~1.1 MB
📊 短语: 2,500+

[📥 立即下载]

─────── 或 ───────

📂 导入本地文件
从 JSON 文件导入您自己的学术短语

[📁 选择 JSON 文件]
```

3. 测试导入,验证消息:
   - 导入中: "⏳ 导入中..."
   - 成功: "✅ 短语导入成功! (X phrases)"
   - 失败: "❌ 导入失败: ..."

---

## 成功标准

所有以下项目必须通过:

- ✅ UI 正确显示两个选项 (Download / Import)
- ✅ "or" 分隔线正确显示
- ✅ 导入按钮正确触发文件选择器
- ✅ 有效 JSON 成功导入
- ✅ 无效 JSON 显示错误消息
- ✅ 导入成功后显示短语数量
- ✅ 导入的短语可以搜索和查看
- ✅ 中英文切换正常工作
- ✅ Service Worker 日志正确
- ✅ IndexedDB 包含导入的短语

---

## 常见问题

### Q: 点击导入按钮没反应

**解决**:
1. 检查 Console 错误
2. 确保文件类型为 `.json`
3. 重新加载扩展

### Q: 导入后找不到短语

**解决**:
1. 检查 JSON 格式是否正确
2. 确保 `sections` 字段存在
3. 查看 Service Worker Console 日志
4. 在 IndexedDB 中手动查找

### Q: 导入后重复了

**解决**:
这是正常的!`importPhrases()` 会累积导入,不会自动去重。
如果需要清空:
1. Settings → Data Management
2. Clear Database (功能待实现)

---

## 下一步

测试通过后:
1. 创建示例 JSON 文件供用户参考
2. 添加数据管理 UI (清空数据库)
3. 添加导入数量限制 (防止导入过大文件)
4. 添加去重功能

---

## 需要帮助?

如果测试失败:
1. 提供 Service Worker Console 日志
2. 提供测试 JSON 文件内容
3. 提供错误截图
