# MyDictionary - 混合模型策略设计

## 一、核心设计理念

### 1.1 智能模型选择
**专用模型优先 + 通用模型兜底**

```
用户翻译请求
    ↓
检测语言对 (如: 英→中)
    ↓
是否为用户设置的主要语言对?
    ├─ 是 → 使用专用高质量模型 (Helsinki-NLP/opus-mt-*)
    │         + 提供近义词、例句功能
    └─ 否 → 使用通用多语言模型 (NLLB-200)
              (仅翻译功能,无近义词/例句)
```

### 1.2 用户交互入口

#### 方式 1: 网页划词
```
用户在网页选中文本
    ↓
显示翻译图标/右键菜单
    ↓
点击后滑入右侧面板
    ↓
自动填充选中文本 + 显示翻译结果
```

#### 方式 2: 点击插件图标
```
用户点击浏览器工具栏的插件图标
    ↓
右侧滑入面板
    ↓
显示空白输入框
    ↓
用户手动输入文本 + 点击翻译
```

---

## 二、模型分层架构

### 2.1 三层模型体系

```javascript
const MODEL_ARCHITECTURE = {
  // 第一层: 专用高质量模型 (用户主要语言对)
  DEDICATED: {
    'en-zh': {
      translation: 'Helsinki-NLP/opus-mt-en-zh',  // 300MB
      synonyms: 'Xenova/distilbert-base-uncased', // 65MB
      examples: 'Xenova/all-MiniLM-L6-v2'         // 23MB
    },
    'zh-en': {
      translation: 'Helsinki-NLP/opus-mt-zh-en',  // 300MB
      synonyms: 'Xenova/distilbert-base-uncased', // 65MB (共享)
      examples: 'Xenova/all-MiniLM-L6-v2'         // 23MB (共享)
    }
  },

  // 第二层: 通用多语言模型 (覆盖所有其他语言对)
  UNIVERSAL: {
    translation: 'Xenova/nllb-200-distilled-600M' // 600MB
  },

  // 第三层: 可选专业模型 (用户可自行下载)
  PROFESSIONAL: {
    'ja-zh': 'Helsinki-NLP/opus-mt-ja-zh',        // 日中翻译
    'ko-en': 'Helsinki-NLP/opus-mt-ko-en',        // 韩英翻译
    // ... 用户按需添加
  }
};
```

### 2.2 默认安装策略

**首次安装时,用户选择主要语言对**:

```
┌─────────────────────────────────────┐
│  🎉 欢迎使用 MyDictionary!          │
├─────────────────────────────────────┤
│                                     │
│  请选择您的主要翻译语言对:           │
│                                     │
│  我的母语:                           │
│  ┌───────────────────────────────┐ │
│  │ 🇨🇳 中文  ▼                   │ │
│  └───────────────────────────────┘ │
│                                     │
│  最常翻译的语言:                     │
│  ┌───────────────────────────────┐ │
│  │ 🇺🇸 English ▼                 │ │
│  └───────────────────────────────┘ │
│                                     │
│  📦 将下载以下模型:                  │
│  ✅ 中文→英文专用模型 (300MB)        │
│  ✅ 英文→中文专用模型 (300MB)        │
│  ✅ 近义词模型 (65MB)                │
│  ✅ 例句模型 (23MB)                  │
│  ✅ 通用多语言模型 (600MB)            │
│                                     │
│  总计: ~1.3GB                        │
│                                     │
│  ℹ️ 专用模型提供更高质量的翻译        │
│  ℹ️ 通用模型支持其他 198 种语言      │
│                                     │
│  [开始下载]  [高级设置]              │
│                                     │
└─────────────────────────────────────┘
```

**高级设置选项**:

```
┌─────────────────────────────────────┐
│  ⚙️ 高级模型设置                     │
├─────────────────────────────────────┤
│                                     │
│  下载策略:                           │
│  ○ 完整安装 (~1.3GB)                 │
│     • 专用模型 + 通用模型             │
│     • 最佳翻译质量                   │
│     • 支持所有功能                   │
│                                     │
│  ◉ 精简安装 (~688MB) 推荐            │
│     • 仅专用模型                     │
│     • 高质量主要语言对翻译            │
│     • 其他语言需在线翻译              │
│                                     │
│  ○ 最小安装 (~600MB)                 │
│     • 仅通用模型                     │
│     • 支持 200 种语言                │
│     • 翻译质量略低                   │
│                                     │
│  [确认]  [返回]                      │
│                                     │
└─────────────────────────────────────┘
```

---

## 三、智能模型路由

### 3.1 路由决策树

```javascript
// background.js - 智能模型选择器

class ModelRouter {
  constructor() {
    this.dedicatedModels = new Map(); // 专用模型缓存
    this.universalModel = null;       // 通用模型
    this.userPreferences = null;      // 用户设置
  }

  /**
   * 初始化:加载用户设置
   */
  async initialize() {
    const settings = await chrome.storage.local.get('modelPreferences');
    this.userPreferences = settings.modelPreferences || {
      primaryLanguagePair: ['en', 'zh'], // 默认英中
      useUniversalFallback: true         // 启用通用模型兜底
    };
  }

  /**
   * 核心路由逻辑
   */
  async route(sourceLang, targetLang) {
    const pair = `${sourceLang}-${targetLang}`;

    // 1. 检查是否为主要语言对
    if (this.isPrimaryPair(sourceLang, targetLang)) {
      return this.getDedicatedModel(pair);
    }

    // 2. 检查是否有可选的专业模型
    if (this.dedicatedModels.has(pair)) {
      return this.getDedicatedModel(pair);
    }

    // 3. 使用通用模型
    if (this.universalModel) {
      return {
        type: 'UNIVERSAL',
        model: this.universalModel,
        capabilities: ['translation'] // 仅翻译,无近义词/例句
      };
    }

    // 4. 未安装任何模型
    throw new Error('NO_MODEL_AVAILABLE');
  }

  /**
   * 判断是否为用户主要语言对
   */
  isPrimaryPair(sourceLang, targetLang) {
    const [lang1, lang2] = this.userPreferences.primaryLanguagePair;
    return (
      (sourceLang === lang1 && targetLang === lang2) ||
      (sourceLang === lang2 && targetLang === lang1)
    );
  }

  /**
   * 获取专用模型
   */
  async getDedicatedModel(pair) {
    if (!this.dedicatedModels.has(pair)) {
      // 懒加载专用模型
      await this.loadDedicatedModel(pair);
    }

    return {
      type: 'DEDICATED',
      model: this.dedicatedModels.get(pair).translation,
      synonymsModel: this.dedicatedModels.get(pair).synonyms,
      examplesModel: this.dedicatedModels.get(pair).examples,
      capabilities: ['translation', 'synonyms', 'examples']
    };
  }

  /**
   * 加载专用模型
   */
  async loadDedicatedModel(pair) {
    const modelConfig = MODEL_CONFIGS.dedicated[pair];
    if (!modelConfig) {
      throw new Error(`No dedicated model config for ${pair}`);
    }

    const translationPipeline = await pipeline(
      'translation',
      modelConfig.translation
    );

    let synonymsPipeline = null;
    let examplesPipeline = null;

    // 如果是英文相关语言对,加载近义词和例句模型
    if (pair.includes('en')) {
      synonymsPipeline = await pipeline(
        'fill-mask',
        modelConfig.synonyms
      );
      examplesPipeline = await pipeline(
        'feature-extraction',
        modelConfig.examples
      );
    }

    this.dedicatedModels.set(pair, {
      translation: translationPipeline,
      synonyms: synonymsPipeline,
      examples: examplesPipeline
    });
  }
}
```

### 3.2 翻译请求处理

```javascript
// background.js

const modelRouter = new ModelRouter();

chrome.runtime.onMessage.addListener((request, sender, sendResponse) => {
  if (request.action === 'translate') {
    handleTranslation(request, sendResponse);
    return true; // 异步响应
  }
});

async function handleTranslation(request, sendResponse) {
  const { text, sourceLang, targetLang } = request;

  try {
    // 1. 路由到合适的模型
    const modelInfo = await modelRouter.route(sourceLang, targetLang);

    // 2. 执行翻译
    const translation = await modelInfo.model(text, {
      src_lang: LANGUAGE_CODES[sourceLang],
      tgt_lang: LANGUAGE_CODES[targetLang]
    });

    // 3. 返回结果,包含可用功能提示
    sendResponse({
      success: true,
      data: {
        translation: translation[0].translation_text,
        modelType: modelInfo.type,
        availableFeatures: modelInfo.capabilities,
        message: modelInfo.type === 'DEDICATED'
          ? '使用专用高质量模型翻译'
          : '使用通用模型翻译(无近义词和例句功能)'
      }
    });

  } catch (error) {
    if (error.message === 'NO_MODEL_AVAILABLE') {
      sendResponse({
        success: false,
        error: 'MODEL_NOT_INSTALLED',
        action: {
          type: 'SUGGEST_DOWNLOAD',
          suggestions: [
            {
              name: `专用模型 (${sourceLang}-${targetLang})`,
              size: '300MB',
              quality: 'high'
            },
            {
              name: '通用多语言模型',
              size: '600MB',
              quality: 'medium',
              coverage: '200 languages'
            }
          ]
        }
      });
    } else {
      sendResponse({ success: false, error: error.message });
    }
  }
}
```

---

## 四、用户界面设计

### 4.1 点击插件图标 - 空白输入模式

```
用户点击浏览器工具栏的插件图标
    ↓
右侧滑入面板 (空白状态)


┌─────────────────────────────────────┐
│  ✕  MyDictionary                🌙 │
├─────────────────────────────────────┤
│                                     │
│  从                                  │
│  ┌───────────────────────────────┐ │
│  │ 🌐 自动检测  ▼                │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │                               │ │
│  │  在此输入文本...               │ │
│  │                               │ │
│  │                               │ │
│  └───────────────────────────────┘ │
│                  [翻译] [清空]      │
│                                     │
│  翻译为                              │
│  ┌───────────────────────────────┐ │
│  │ 🇨🇳 中文  ▼                   │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │                               │ │
│  │  翻译结果将显示在这里...       │ │
│  │                               │ │
│  │                               │ │
│  └───────────────────────────────┘ │
│                                     │
│  ─────────────────────────────────  │
│                                     │
│  💡 快速开始:                        │
│  • 选中网页文本 + Ctrl+Shift+F       │
│  • 或直接在上方输入框输入            │
│                                     │
└─────────────────────────────────────┘
```

### 4.2 网页划词 - 自动填充模式

```
用户在网页选中 "artificial intelligence"
    ↓
按下 Ctrl+Shift+F
    ↓
右侧滑入面板 (自动填充选中文本)


┌─────────────────────────────────────┐
│  ✕  MyDictionary                🌙 │
├─────────────────────────────────────┤
│                                     │
│  从 🇺🇸 English (已检测)             │
│  ┌───────────────────────────────┐ │
│  │ artificial intelligence       │ │ ← 自动填充
│  └───────────────────────────────┘ │
│                  [重新翻译] [清空]  │
│                                     │
│  翻译为 🇨🇳 中文                     │
│  ┌───────────────────────────────┐ │
│  │ 人工智能                       │ │ ← 自动翻译
│  └───────────────────────────────┘ │
│                                     │
│  📝 上下文                           │
│  "The rapid development of          │
│   artificial intelligence is..."   │
│                                     │
│  ─────────────────────────────────  │
│  [📚 近义词]    [💬 例句]            │
│                                     │
│  ✨ 使用专用高质量模型翻译            │
│                                     │
└─────────────────────────────────────┘
```

### 4.3 使用通用模型时的提示

```
用户翻译 英文 → 泰语


┌─────────────────────────────────────┐
│  ✕  MyDictionary                🌙 │
├─────────────────────────────────────┤
│                                     │
│  从 🇺🇸 English                      │
│  ┌───────────────────────────────┐ │
│  │ Hello, how are you?           │ │
│  └───────────────────────────────┘ │
│                                     │
│  翻译为 🇹🇭 ไทย                      │
│  ┌───────────────────────────────┐ │
│  │ สวัสดี คุณเป็นอย่างไร?       │ │
│  └───────────────────────────────┘ │
│                                     │
│  ⚠️ 使用通用模型翻译                 │
│  ℹ️ 近义词和例句功能仅支持主要语言对  │
│                                     │
│  💡 想要更高质量的英泰翻译?           │
│  [下载专用模型 (300MB)]              │
│                                     │
└─────────────────────────────────────┘
```

---

## 五、模型下载管理

### 5.1 动态下载界面

**设置页面 > 模型管理**

```
┌─────────────────────────────────────┐
│  📦 模型管理                         │
├─────────────────────────────────────┤
│                                     │
│  主要语言对: 🇺🇸 ⇄ 🇨🇳                │
│                                     │
│  专用模型                            │
│  ┌───────────────────────────────┐ │
│  │ 🇺🇸 → 🇨🇳 英译中               │ │
│  │    状态: ✅ 已加载 (300MB)     │ │
│  │    质量: ⭐⭐⭐⭐⭐               │ │
│  │    功能: 翻译 + 近义词 + 例句   │ │
│  │    [查看详情]  [卸载]          │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 🇨🇳 → 🇺🇸 中译英               │ │
│  │    状态: ✅ 已加载 (300MB)     │ │
│  │    质量: ⭐⭐⭐⭐⭐               │ │
│  │    功能: 翻译 + 近义词 + 例句   │ │
│  │    [查看详情]  [卸载]          │ │
│  └───────────────────────────────┘ │
│                                     │
│  通用模型                            │
│  ┌───────────────────────────────┐ │
│  │ 🌐 多语言翻译 (NLLB-200)       │ │
│  │    状态: ✅ 已加载 (600MB)     │ │
│  │    支持: 200 种语言            │ │
│  │    质量: ⭐⭐⭐⭐                │ │
│  │    功能: 仅翻译                │ │
│  │    [查看详情]  [卸载]          │ │
│  └───────────────────────────────┘ │
│                                     │
│  可选模型                            │
│  ┌───────────────────────────────┐ │
│  │ 🇯🇵 ⇄ 🇨🇳 日中翻译专用          │ │
│  │    状态: ⬇️ 未安装             │ │
│  │    大小: 300MB                │ │
│  │    [立即下载]                  │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 🇰🇷 → 🇺🇸 韩英翻译专用          │ │
│  │    状态: ⬇️ 未安装             │ │
│  │    大小: 300MB                │ │
│  │    [立即下载]                  │ │
│  └───────────────────────────────┘ │
│                                     │
│  ℹ️ 已使用存储空间: 1.2GB            │
│                                     │
│  [更改主要语言对]  [清除所有缓存]    │
│                                     │
└─────────────────────────────────────┘
```

### 5.2 更改主要语言对

```
┌─────────────────────────────────────┐
│  ⚙️ 更改主要语言对                   │
├─────────────────────────────────────┤
│                                     │
│  当前主要语言对: 🇺🇸 ⇄ 🇨🇳           │
│                                     │
│  选择新的主要语言对:                 │
│                                     │
│  语言 1:                             │
│  ┌───────────────────────────────┐ │
│  │ 🇺🇸 English  ▼                │ │
│  └───────────────────────────────┘ │
│                                     │
│  语言 2:                             │
│  ┌───────────────────────────────┐ │
│  │ 🇯🇵 日本語  ▼                  │ │
│  └───────────────────────────────┘ │
│                                     │
│  ⚠️ 更改后将执行以下操作:             │
│  • 卸载当前专用模型 (释放 600MB)     │
│  • 下载新的专用模型:                 │
│    - 英→日 (300MB)                  │
│    - 日→英 (300MB)                  │
│                                     │
│  预计下载时间: 约 2 分钟              │
│                                     │
│  [确认更改]  [取消]                  │
│                                     │
└─────────────────────────────────────┘
```

---

## 六、技术实现要点

### 6.1 模型配置文件

```json
// models-config.json
{
  "dedicatedModels": {
    "en-zh": {
      "translation": "Helsinki-NLP/opus-mt-en-zh",
      "synonyms": "Xenova/distilbert-base-uncased",
      "examples": "Xenova/all-MiniLM-L6-v2",
      "size": 388,
      "quality": 5
    },
    "zh-en": {
      "translation": "Helsinki-NLP/opus-mt-zh-en",
      "synonyms": "Xenova/distilbert-base-uncased",
      "examples": "Xenova/all-MiniLM-L6-v2",
      "size": 388,
      "quality": 5
    },
    "ja-zh": {
      "translation": "Helsinki-NLP/opus-mt-ja-zh",
      "size": 300,
      "quality": 4
    },
    "ko-en": {
      "translation": "Helsinki-NLP/opus-mt-ko-en",
      "size": 300,
      "quality": 4
    }
  },
  "universalModel": {
    "translation": "Xenova/nllb-200-distilled-600M",
    "size": 600,
    "quality": 4,
    "supportedLanguages": 200
  }
}
```

### 6.2 用户设置存储

```javascript
// chrome.storage.local 数据结构
{
  "modelPreferences": {
    "primaryLanguagePair": ["en", "zh"],
    "installationType": "FULL", // "FULL" | "LITE" | "MINIMAL"
    "installedModels": {
      "en-zh": true,
      "zh-en": true,
      "universal": true
    },
    "enabledFeatures": {
      "synonyms": true,
      "examples": true
    }
  },
  "userSettings": {
    "defaultSourceLanguage": "auto",
    "defaultTargetLanguage": "zh",
    "autoDetectLanguage": true
  }
}
```

### 6.3 性能优化

```javascript
// 共享近义词和例句模型,避免重复加载
class ModelManager {
  constructor() {
    this.sharedModels = {
      synonyms: null,   // 全局共享
      examples: null    // 全局共享
    };
  }

  async loadSynonymsModel() {
    if (!this.sharedModels.synonyms) {
      console.log('加载共享近义词模型...');
      this.sharedModels.synonyms = await pipeline(
        'fill-mask',
        'Xenova/distilbert-base-uncased'
      );
    }
    return this.sharedModels.synonyms;
  }

  async loadExamplesModel() {
    if (!this.sharedModels.examples) {
      console.log('加载共享例句模型...');
      this.sharedModels.examples = await pipeline(
        'feature-extraction',
        'Xenova/all-MiniLM-L6-v2'
      );
    }
    return this.sharedModels.examples;
  }
}
```

---

## 七、总结对比

### 方案对比

| 特性 | 单模型方案 | 混合模型方案 (推荐) |
|------|-----------|-------------------|
| **翻译质量(主要语言对)** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **翻译质量(其他语言)** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **近义词功能** | ✅ 仅英文 | ✅ 仅英文 |
| **例句功能** | ✅ 仅英文 | ✅ 仅英文 |
| **支持语言数** | 200 种 | 200 种 |
| **初始下载大小** | 600MB | 688-1288MB |
| **用户体验** | 简单 | 灵活可控 |
| **适用场景** | 偶尔使用多种语言 | 主要使用 1-2 个语言对 |

### 推荐安装类型

**完整安装 (~1.3GB)** - 最佳体验
- ✅ 专用模型 (英中双向 600MB)
- ✅ 通用模型 (600MB)
- ✅ 近义词 + 例句 (88MB)

**精简安装 (~688MB)** - 推荐
- ✅ 专用模型 (英中双向 600MB)
- ✅ 近义词 + 例句 (88MB)
- ❌ 通用模型 (其他语言需在线翻译)

**最小安装 (~600MB)**
- ❌ 专用模型
- ✅ 通用模型 (600MB)
- ❌ 近义词 + 例句
