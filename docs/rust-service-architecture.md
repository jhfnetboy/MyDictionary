# Rust 服务模块架构设计

## 📋 项目定位

**名称**: `model-runner` (Rust 服务模块)

**目标**:
- 🎯 **当前**: 基础的模型下载和服务运行功能
- 🚀 **未来**: Hugging Face 模型卡片列表 + 下载运行试验场

**核心价值**:
- ⚡ 高性能模型推理 (Rust + Candle)
- 🔌 插件化架构 (可扩展到多种模型)
- 🎨 可视化模型管理 (Web UI)

---

## 🏗️ 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                   MyDictionary (Chrome Extension)       │
│                                                          │
│  ┌────────────┐    ┌────────────┐    ┌────────────┐   │
│  │ Translation│    │   TTS UI   │    │  Academic  │   │
│  │     UI     │    │            │    │  Writing   │   │
│  └────────────┘    └────────────┘    └────────────┘   │
│         │                 │                  │          │
│         └─────────────────┴──────────────────┘          │
│                         │                                │
│                    HTTP Request                          │
└─────────────────────────┼───────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              Python TTS Server (Flask)                  │
│              HTTP API Gateway (Port 5050)               │
│                                                          │
│  ├─ GET  /                  (服务器信息)                │
│  ├─ GET  /models            (模型列表)                  │
│  ├─ POST /models/:id/load   (加载模型)                  │
│  ├─ POST /synthesize        (TTS 合成)                  │
│  └─ GET  /health            (健康检查)                  │
│                                                          │
│         │                                                │
│         │ gRPC / HTTP (内部通信)                         │
│         ▼                                                │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│         Rust Model Runner (Candle + Hugging Face)       │
│                                                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │  Core Engine                                     │   │
│  │                                                   │   │
│  │  ├─ Model Downloader (下载管理)                  │   │
│  │  │   └─ Hugging Face Hub API                     │   │
│  │  │                                                │   │
│  │  ├─ Model Loader (模型加载)                      │   │
│  │  │   └─ Candle Framework                         │   │
│  │  │                                                │   │
│  │  ├─ Model Runner (推理引擎)                      │   │
│  │  │   ├─ TTS Models (SpeechT5, CosyVoice)         │   │
│  │  │   ├─ Translation Models (NLLB-200)            │   │
│  │  │   └─ Embedding Models (BGE)                   │   │
│  │  │                                                │   │
│  │  └─ Model Cache (缓存管理)                       │   │
│  │      └─ ~/.cache/huggingface                     │   │
│  └─────────────────────────────────────────────────┘   │
│                                                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │  Plugin System (插件系统)                        │   │
│  │                                                   │   │
│  │  ├─ Plugin: TTS                                  │   │
│  │  │   ├─ SpeechT5                                 │   │
│  │  │   └─ CosyVoice                                │   │
│  │  │                                                │   │
│  │  ├─ Plugin: Translation                          │   │
│  │  │   └─ NLLB-200                                 │   │
│  │  │                                                │   │
│  │  └─ Plugin: Embedding                            │   │
│  │      └─ BGE-base                                 │   │
│  └─────────────────────────────────────────────────┘   │
│                                                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │  Web UI (Dioxus/Leptos - 未来)                  │   │
│  │                                                   │   │
│  │  ├─ Model Browser (模型卡片列表)                 │   │
│  │  │   └─ Search, Filter, Sort                     │   │
│  │  │                                                │   │
│  │  ├─ Model Playground (试验场)                    │   │
│  │  │   ├─ Input Text                               │   │
│  │  │   ├─ Run Inference                            │   │
│  │  │   └─ View Output                              │   │
│  │  │                                                │   │
│  │  └─ Settings (配置管理)                          │   │
│  │      ├─ Model Storage Path                       │   │
│  │      ├─ Device Selection (CPU/GPU)               │   │
│  │      └─ Cache Management                         │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

---

## 📦 技术栈选择

### Rust Core

```toml
[dependencies]
# 核心推理框架
candle-core = "0.8"           # Hugging Face Candle
candle-nn = "0.8"
candle-transformers = "0.8"

# HTTP 服务 (gRPC 或 REST)
tonic = "0.12"                # gRPC server
axum = "0.7"                  # HTTP server (备选)

# 异步运行时
tokio = { version = "1", features = ["full"] }

# Hugging Face Hub
hf-hub = "0.3"                # 模型下载

# 序列化
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# 日志
tracing = "0.1"
tracing-subscriber = "0.3"

# 错误处理
anyhow = "1"
thiserror = "1"

# 配置管理
config = "0.14"

# 未来: Web UI
# dioxus = "0.6"              # Rust Web UI (未来)
# leptos = "0.7"              # Rust Web UI (备选)
```

### Python Gateway

```python
# requirements.txt
flask==3.1.2
flask-cors==6.0.1
grpcio==1.67.0        # 与 Rust 通信
grpcio-tools==1.67.0  # gRPC 代码生成
```

---

## 🎯 MVP 功能 (第一阶段)

### 1. 模型下载器

**功能**:
- ✅ 从 Hugging Face Hub 下载模型
- ✅ 显示下载进度
- ✅ 断点续传
- ✅ 缓存管理

**API**:
```rust
// Rust
pub struct ModelDownloader {
    cache_dir: PathBuf,
    hf_api: HfApi,
}

impl ModelDownloader {
    pub async fn download_model(
        &self,
        model_id: &str,
        progress_callback: impl Fn(f32)
    ) -> Result<PathBuf> {
        // 下载逻辑
    }
}
```

### 2. TTS 推理引擎

**支持模型**:
- ✅ SpeechT5 (英文)
- 🔜 CosyVoice (中英文)

**API**:
```rust
pub struct TTSEngine {
    model: Model,
    vocoder: Model,
}

impl TTSEngine {
    pub fn synthesize(
        &self,
        text: &str,
        speaker_id: u32
    ) -> Result<Vec<f32>> {
        // TTS 推理
    }
}
```

### 3. gRPC 服务

**Proto 定义**:
```protobuf
// tts.proto
syntax = "proto3";

service TTSService {
  rpc LoadModel(LoadModelRequest) returns (LoadModelResponse);
  rpc Synthesize(SynthesizeRequest) returns (SynthesizeResponse);
  rpc GetStatus(Empty) returns (StatusResponse);
}

message LoadModelRequest {
  string model_id = 1;  // "microsoft/speecht5_tts"
}

message SynthesizeRequest {
  string text = 1;
  uint32 speaker_id = 2;
  string format = 3;  // "wav" | "mp3"
}

message SynthesizeResponse {
  bytes audio_data = 1;
  uint32 sample_rate = 2;
}
```

---

## 📁 项目结构

```
model-runner/
├── Cargo.toml
├── proto/
│   └── tts.proto              # gRPC 接口定义
├── src/
│   ├── main.rs                # 服务入口
│   ├── config.rs              # 配置管理
│   ├── downloader/            # 模型下载模块
│   │   ├── mod.rs
│   │   ├── hf_hub.rs          # Hugging Face Hub API
│   │   └── cache.rs           # 缓存管理
│   ├── models/                # 模型定义
│   │   ├── mod.rs
│   │   ├── tts/
│   │   │   ├── speecht5.rs
│   │   │   └── cosyvoice.rs   # 未来
│   │   ├── translation/
│   │   │   └── nllb.rs        # 未来
│   │   └── embedding/
│   │       └── bge.rs         # 未来
│   ├── runner/                # 推理引擎
│   │   ├── mod.rs
│   │   ├── engine.rs
│   │   └── device.rs          # CPU/GPU 管理
│   ├── server/                # gRPC 服务
│   │   ├── mod.rs
│   │   └── tts_service.rs
│   └── ui/                    # Web UI (未来)
│       └── mod.rs
├── tests/
│   └── integration_test.rs
└── examples/
    └── simple_tts.rs          # 使用示例
```

---

## 🚀 实现路线图

### Phase 1: 基础框架 (MVP)

**目标**: 替换 Python TTS，实现基础推理

**任务**:
- [x] 项目初始化 (Cargo + Proto)
- [ ] 实现 ModelDownloader
  - [ ] Hugging Face Hub 下载
  - [ ] 进度回调
  - [ ] 缓存管理
- [ ] 实现 SpeechT5 推理
  - [ ] 模型加载
  - [ ] 文本预处理
  - [ ] 音频生成
- [ ] gRPC 服务
  - [ ] Proto 编译
  - [ ] 服务实现
  - [ ] 与 Python Gateway 集成
- [ ] 基础测试
  - [ ] 单元测试
  - [ ] 集成测试
  - [ ] 性能测试

**交付物**:
```bash
# 启动 Rust 服务
./model-runner serve --port 50051

# Python Gateway 调用
python server.py  # 自动连接到 Rust gRPC
```

### Phase 2: 模型扩展

**目标**: 支持更多模型

**任务**:
- [ ] CosyVoice 集成 (中文 TTS)
- [ ] NLLB-200 集成 (翻译)
- [ ] BGE 集成 (语义搜索)
- [ ] 插件系统设计
- [ ] 配置文件管理

**交付物**:
```toml
# config.toml
[models.tts]
engine = "cosyvoice"  # 或 "speecht5"

[models.translation]
engine = "nllb"

[models.embedding]
engine = "bge"
```

### Phase 3: Web UI (试验场)

**目标**: 可视化模型管理

**任务**:
- [ ] Hugging Face 模型搜索
- [ ] 模型卡片展示
- [ ] 一键下载/加载
- [ ] Playground 界面
  - [ ] 输入文本
  - [ ] 实时推理
  - [ ] 结果展示
- [ ] 设置页面
  - [ ] 存储路径
  - [ ] 设备选择
  - [ ] 缓存清理

**交付物**:
```bash
# 启动 Web UI
./model-runner ui --port 8080

# 浏览器访问
open http://localhost:8080
```

---

## 🎨 未来愿景: Hugging Face 模型试验场

### 核心功能

**1. 模型浏览器**
```
┌──────────────────────────────────────────────────┐
│  🔍 Search: "tts chinese"              [Filter ▼]│
├──────────────────────────────────────────────────┤
│  📦 FunAudioLLM/CosyVoice-300M        ⭐ 1.2k    │
│     Text-to-Speech | Chinese, English            │
│     [Download] [Load] [Try]                      │
├──────────────────────────────────────────────────┤
│  📦 microsoft/speecht5_tts            ⭐ 856     │
│     Text-to-Speech | English                     │
│     ✅ Loaded  [Unload] [Try]                    │
├──────────────────────────────────────────────────┤
│  📦 alibaba-damo/speech_paraformer_asr ⭐ 432    │
│     Speech Recognition | Chinese                 │
│     [Download] [Load] [Try]                      │
└──────────────────────────────────────────────────┘
```

**2. 模型 Playground**
```
┌──────────────────────────────────────────────────┐
│  Model: CosyVoice-300M               [Settings ⚙]│
├──────────────────────────────────────────────────┤
│  Input:                                           │
│  ┌────────────────────────────────────────────┐  │
│  │ 你好，这是一个测试。                       │  │
│  │ Hello, this is a test.                     │  │
│  └────────────────────────────────────────────┘  │
│                                                   │
│  Speaker: [Female 1 ▼]  Speed: [1.0x]            │
│                                                   │
│  [▶ Run]  [⏹ Stop]  [💾 Save]                    │
├──────────────────────────────────────────────────┤
│  Output:                                          │
│  🔊 ═══════════════════▶ 0:03 / 0:03             │
│                                                   │
│  Inference Time: 1.2s                             │
│  Quality: High (9/10)                             │
└──────────────────────────────────────────────────┘
```

**3. 模型对比**
```
┌──────────────────────────────────────────────────┐
│  Compare Models                                   │
├──────────────────────────────────────────────────┤
│  Input: "Hello World"                             │
│                                                   │
│  ┌─────────────────┬──────────────────────────┐  │
│  │ SpeechT5        │ CosyVoice                │  │
│  ├─────────────────┼──────────────────────────┤  │
│  │ 🔊 0:01         │ 🔊 0:01                  │  │
│  │ Speed: 0.8s     │ Speed: 1.2s              │  │
│  │ Quality: 6/10   │ Quality: 9/10            │  │
│  │ Memory: 500MB   │ Memory: 2GB              │  │
│  └─────────────────┴──────────────────────────┘  │
└──────────────────────────────────────────────────┘
```

---

## 📊 性能目标

| 指标 | 目标 | 说明 |
|------|------|------|
| 模型下载速度 | > 5MB/s | Hugging Face CDN |
| 首次加载时间 | < 3s | SpeechT5 |
| 推理延迟 | < 1s | 10 词以内 |
| 内存占用 | < 2GB | CosyVoice |
| CPU 占用 | < 50% | 单核 |
| 并发请求 | > 10 | gRPC |

---

## 🔧 开发工具

```bash
# 代码格式化
cargo fmt

# 代码检查
cargo clippy

# 运行测试
cargo test

# 性能测试
cargo bench

# 构建发布版本
cargo build --release

# gRPC 代码生成
./scripts/generate_proto.sh
```

---

## 📚 参考资源

- [Candle Framework](https://github.com/huggingface/candle)
- [Hugging Face Hub Rust](https://github.com/huggingface/hf-hub)
- [tonic gRPC](https://github.com/hyperium/tonic)
- [Dioxus Web UI](https://dioxuslabs.com/)
- [SpeechT5 论文](https://arxiv.org/abs/2110.07205)
- [CosyVoice 模型卡](https://huggingface.co/FunAudioLLM/CosyVoice-300M)

---

**版本**: v1.0-draft
**更新日期**: 2025-11-30
**状态**: 📝 规划中
