# TTS 自动发现和智能切换架构

## 📋 概述

MyDictionary TTS 现在支持**自动发现本地 Rust 服务器**,并在服务器不可用时**智能回退到浏览器 TTS**。

这个设计让用户无需任何配置,即可享受:
- ✅ **本地服务器可用时**: 高质量 TTS (未来支持中文,更大音量)
- ✅ **本地服务器不可用时**: 自动使用浏览器 SpeechT5 (英文 TTS)
- ✅ **完全透明**: 用户无感知切换,无需手动配置

---

## 🏗️ 架构设计

```
┌─────────────────────────────────────────────────────────────┐
│                  Chrome Extension (Service Worker)          │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              TTSManager (tts-manager.js)                │ │
│  │                                                          │ │
│  │  ┌──────────────────────────────────────────────────┐  │ │
│  │  │  speak(text) - 智能播放入口                       │  │ │
│  │  │                                                    │  │ │
│  │  │  1. checkLocalServer() (30秒缓存)                 │  │ │
│  │  │     └─ GET http://localhost:3030/health (1秒超时) │  │ │
│  │  │                                                    │  │ │
│  │  │  2. if (服务器可用) {                              │  │ │
│  │  │       try { speakViaLocalServer() }               │  │ │
│  │  │       catch { fallback to browser }               │  │ │
│  │  │     }                                              │  │ │
│  │  │                                                    │  │ │
│  │  │  3. else {                                         │  │ │
│  │  │       speakViaBrowser()  // SpeechT5             │  │ │
│  │  │     }                                              │  │ │
│  │  └──────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                             │
                             │ HTTP Request
                             ▼
┌─────────────────────────────────────────────────────────────┐
│           Rust Model Runner (Port 3030)                     │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  Axum HTTP Server (model-runner/src/main.rs)          │ │
│  │                                                          │ │
│  │  GET  /           → Server Info                        │ │
│  │  GET  /health     → { success: true }                  │ │
│  │  POST /synthesize → TTS Audio (WAV)                    │ │
│  │  POST /models/download → Download HF Models            │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  📦 Dependencies:                                            │
│     - Tokio (Async Runtime)                                 │
│     - Axum (HTTP Framework)                                 │
│     - hf-hub (Hugging Face Model Downloader)                │
│     - Candle (ML Framework - 待集成)                         │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔍 核心逻辑: 自动发现

### 1. 健康检查 (checkLocalServer)

```javascript
async checkLocalServer() {
  const now = Date.now();

  // 避免频繁检查 - 30秒缓存
  if (now - this.lastServerCheck < this.serverCheckInterval) {
    return this.localServerAvailable;  // 返回缓存结果
  }

  this.lastServerCheck = now;

  try {
    const controller = new AbortController();
    const timeoutId = setTimeout(() => controller.abort(), 1000); // 1秒超时

    const response = await fetch(`${this.localServerUrl}/health`, {
      method: 'GET',
      signal: controller.signal
    });

    clearTimeout(timeoutId);

    if (response.ok) {
      const data = await response.json();
      this.localServerAvailable = true;
      console.log('✅ 本地 TTS 服务器可用:', data);
      return true;
    }
  } catch (error) {
    // 服务器不可用 (正常情况)
    if (this.localServerAvailable) {
      console.log('⚠️ 本地 TTS 服务器已断开');
    }
    this.localServerAvailable = false;
  }

  return false;
}
```

**关键设计点**:
- ✅ **30秒缓存**: 避免每次 TTS 都检查,减少延迟
- ✅ **1秒超时**: 快速失败,不阻塞用户操作
- ✅ **AbortController**: 超时后立即中断请求
- ✅ **静默失败**: 服务器不可用是正常情况,不报错

### 2. 智能播放 (speak)

```javascript
async speak(text, onEnd = null, onError = null) {
  try {
    if (this.isPlaying) {
      this.stop();
    }

    // 检查本地服务器 (带缓存)
    const hasLocalServer = await this.checkLocalServer();

    if (hasLocalServer) {
      // 优先使用本地服务器
      console.log('🎵 使用本地 Rust TTS 服务器');
      try {
        await this.speakViaLocalServer(text, onEnd, onError);
        return; // 成功则直接返回
      } catch (localError) {
        console.warn('⚠️ 本地服务器失败,回退到浏览器 TTS:', localError.message);
        // 标记服务器不可用,下次直接用浏览器
        this.localServerAvailable = false;
        // 继续使用浏览器 TTS
      }
    }

    // 使用浏览器 TTS (回退或默认)
    console.log('🎵 使用浏览器 TTS (SpeechT5)');
    await this.speakViaBrowser(text, onEnd, onError);

  } catch (error) {
    console.error('❌ 播放失败:', error);
    this.isPlaying = false;
    if (onError) onError(error);
    throw error;
  }
}
```

**回退策略**:
1. 先检查本地服务器 (30秒缓存)
2. 如果可用 → 尝试本地 TTS
3. 如果本地失败 → **自动回退**到浏览器 TTS
4. 标记服务器不可用,下次直接用浏览器 (避免重复尝试)

---

## 🚀 Rust 服务器架构

### 当前状态: 基础框架完成

**文件结构**:
```
model-runner/
├── Cargo.toml                 # Rust 依赖配置
├── .gitignore                 # 排除 /target/ 构建产物
├── src/
│   ├── main.rs                # HTTP 服务器主程序
│   └── downloader/
│       └── mod.rs             # Hugging Face 模型下载器
```

### API 端点

#### 1. `GET /` - 服务器信息

**请求**:
```bash
curl http://localhost:3030/
```

**响应**:
```json
{
  "success": true,
  "data": {
    "name": "Model Runner",
    "version": "0.1.0",
    "status": "running",
    "mode": "rust-native"
  }
}
```

#### 2. `GET /health` - 健康检查

**请求**:
```bash
curl http://localhost:3030/health
```

**响应**:
```json
{
  "success": true,
  "data": "healthy"
}
```

**用途**: 扩展插件通过此端点检测服务器是否在线

#### 3. `POST /synthesize` - TTS 合成 (当前为模拟模式)

**请求**:
```bash
curl -X POST http://localhost:3030/synthesize \
  -H "Content-Type: application/json" \
  -d '{"text": "Hello World", "format": "wav"}'
```

**当前响应** (模拟模式):
```json
{
  "success": true,
  "data": "模拟 TTS 合成成功: Hello World (format: wav)\n实际音频生成将在 Candle 集成后实现"
}
```

**未来响应** (真实 TTS):
```
Content-Type: audio/wav
Content-Length: 123456

<WAV 音频二进制数据>
```

#### 4. `POST /models/download` - 下载模型

**请求**:
```bash
curl -X POST http://localhost:3030/models/download \
  -H "Content-Type: application/json" \
  -d '{"model_id": "microsoft/speecht5_tts"}'
```

**响应**:
```json
{
  "success": true,
  "data": "模型已下载到: /Users/jason/.cache/huggingface/microsoft--speecht5_tts"
}
```

---

## 📊 性能特性

| 指标 | 健康检查 | 浏览器 TTS | 本地服务器 TTS (未来) |
|------|---------|-----------|---------------------|
| **延迟** | <1 秒 (超时) | ~2 秒 (首次) | ~500ms (预计) |
| **缓存** | 30 秒 | 模型缓存 | 模型预加载 |
| **音量** | - | 偏小 | 正常 |
| **语言** | - | 英文 | 中英文 |
| **质量** | - | 7/10 | 9/10 (预计) |
| **阻塞** | 否 (1秒超时) | 否 (异步) | 否 (异步) |

---

## 🧪 测试指南

### 场景 1: 仅浏览器 TTS (默认)

**前置条件**: Rust 服务器未启动

**测试步骤**:
1. 重新加载 Chrome 扩展
2. 在网页上点击 TTS 按钮 🔊
3. 观察控制台输出

**预期行为**:
```
🔊 TTS 初始化 (自动模式: 本地优先 → 浏览器回退)
🎵 使用浏览器 TTS (SpeechT5)
✅ TTS 模型加载完成!
🎵 音频已发送到 Offscreen Document (浏览器 TTS)
```

### 场景 2: 自动切换到本地服务器

**前置条件**: 启动 Rust 服务器

**启动服务器**:
```bash
cd model-runner
cargo run
```

**测试步骤**:
1. 等待服务器启动 (看到 "🎯 服务器监听地址: 0.0.0.0:3030")
2. 在网页上点击 TTS 按钮 🔊
3. 观察控制台输出

**预期行为**:
```
✅ 本地 TTS 服务器可用: { success: true, data: "healthy" }
🎵 使用本地 Rust TTS 服务器
🎵 音频已发送到 Offscreen Document (本地服务器)
```

### 场景 3: 服务器中断后自动回退

**测试步骤**:
1. Rust 服务器正在运行
2. 点击 TTS 按钮 (应使用本地服务器)
3. 停止 Rust 服务器 (Ctrl+C)
4. **等待 30 秒** (缓存过期)
5. 再次点击 TTS 按钮

**预期行为**:
```
⚠️ 本地 TTS 服务器已断开
🎵 使用浏览器 TTS (SpeechT5)
```

---

## 🔧 配置说明

### 服务器 URL 配置

在 `src/lib/tts-manager.js`:
```javascript
constructor() {
  this.localServerUrl = 'http://localhost:3030'; // Rust 服务器地址
  this.serverCheckInterval = 30000; // 30秒缓存
}
```

**修改端口** (如果需要):
1. 修改 `tts-manager.js` 中的 `localServerUrl`
2. 修改 `model-runner/src/main.rs` 中的端口:
   ```rust
   let addr = SocketAddr::from(([0, 0, 0, 0], 3030)); // 改这里
   ```

### 缓存时间配置

**30 秒缓存** (默认):
- 适合开发和测试
- 服务器启动后最多 30 秒内被发现

**生产环境建议** (60-120 秒):
```javascript
this.serverCheckInterval = 120000; // 2 分钟
```

---

## 📝 开发路线图

### ✅ 已完成 (v0.1.6)

- [x] 自动发现架构设计
- [x] 健康检查实现 (30秒缓存 + 1秒超时)
- [x] 智能回退逻辑
- [x] Rust HTTP 服务器框架
- [x] 模型下载器 (hf-hub 集成)
- [x] CORS 配置 (允许扩展访问)

### 🔜 下一步 (v0.2.0)

**目标**: 实现真实的 Rust TTS 推理

**任务**:
1. 集成 Candle ML 框架
2. 加载 SpeechT5 模型 (ONNX 或 Safetensors)
3. 实现 TTS 推理管道
4. 返回 WAV 音频数据
5. 音量正常化 (解决 SpeechT5 音量小问题)

**预期效果**:
- 本地 TTS 音量正常 (3倍增益)
- 推理速度更快 (Rust 性能优势)
- 支持中文 TTS (CosyVoice 模型)

### 🌟 未来计划 (v0.3.0)

**目标**: Hugging Face 模型试验场

**功能**:
- 🎨 Web UI 模型浏览器
- 📦 一键下载/加载模型
- 🎮 Playground 实时推理
- 🔧 模型参数调节
- 📊 性能对比工具

---

## ❓ 常见问题

### Q1: 为什么要 30 秒缓存?

**A**: 平衡性能和响应速度
- **太短** (如 5 秒): 每次 TTS 都检查,增加延迟
- **太长** (如 5 分钟): 服务器启动后很久才被发现
- **30 秒**: 合理的折中方案

### Q2: 服务器不可用时会报错吗?

**A**: 不会,完全静默回退
- 扩展会自动使用浏览器 TTS
- 控制台仅记录日志 (console.log)
- 用户无感知

### Q3: 如何确认正在使用哪种 TTS?

**A**: 查看浏览器控制台
- `🎵 使用本地 Rust TTS 服务器` → 本地
- `🎵 使用浏览器 TTS (SpeechT5)` → 浏览器

### Q4: Rust 服务器需要一直运行吗?

**A**: 不需要
- 不运行时,扩展自动使用浏览器 TTS
- 运行时,自动切换到本地 TTS
- 完全由用户控制

---

## 📚 相关文档

- [TTS 简化总结](./TTS-simplification-summary.md) - 为什么删除 Python 代码
- [Rust 服务架构](./rust-service-architecture.md) - Rust 服务模块详细设计

---

**版本**: v0.1.6
**更新日期**: 2025-11-30
**状态**: ✅ 基础架构完成,TTS 推理待实现
